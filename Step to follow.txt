DB description:
Entity:- Manager Properties: 
managerid, email, firstname,lastname,password,address(state,city,street,pincode),dob,company etc. 
Entity:- Employee Properties: empId,mId_fk,firstname,lastname,address(state,city,street,pincode),dob,mobile,city etc.

SQL SCRIPT :

Create database manager_employee;

use manager_employee;

create table employee (
       employeeid_pk integer not null auto_increment,
        city varchar(255),
        pin_code varchar(255),
        state varchar(255),
        street varchar(255),
        company varchar(255),
        dob varchar(255),
        email varchar(255),
        firstname varchar(255),
        lastname varchar(255),
        mobile varchar(255),
        password varchar(255),
        managerid_fk integer,
        primary key (employeeid_pk)
    ) engine=InnoDB;
    
    create table manager (
       managerid_pk integer not null auto_increment,
        city varchar(255),
        pin_code varchar(255),
        state varchar(255),
        street varchar(255),
        company varchar(255),
        dob varchar(255),
        email varchar(255),
        firstname varchar(255),
        lastname varchar(255),
        mobile varchar(255),
        password varchar(255),
        primary key (managerid_pk)
    ) engine=InnoDB;
    
      alter table employee
       add constraint FK4m38tcpwau9iockvan4xw1k77
       foreign key (manager_managerid_pk)
       references manager (managerid_pk);

show tables;
desc Manager;
desc Employee;

select * from Manager;
select * from Employee;

insert into manager () values();
________________________________________________________________________________________________________

Create spring boot maven project
add dependencies of spring core, mvc, data jpa, mysql,security, hibernate




CRUD apis and check existing record api

